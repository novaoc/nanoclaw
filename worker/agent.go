package main

// The worker's brain lived here — a full coding agent with a tool belt, a
// design loop, and a vision critic. Every production failure this pipeline
// ever had came out of that brain while the deterministic half worked every
// single time, so on 2026-08-19 it was removed: Vela writes the code, this
// daemon verifies and ships it. This file stays as the tombstone.
